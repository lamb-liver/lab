---
description: "Local AI-assisted code review workflow (Cursor). Not canonical runtime spec — see art.md / p5toreact.md."
alwaysApply: false
---

# 代碼審查（羊·實驗）

> **定位**：Local AI-assisted review workflow（Cursor 手動請求 review 時使用）。  
> **不是** canonical runtime specification。  
> **衝突時**：`src/` 實作 > `art.md` / `p5toreact.md` / `reactkey.md` / `textstyle.md` > 本 rule。

需要細節時再讀 [`art.md`](../../art.md)、[`p5toreact.md`](../../p5toreact.md)；morph 曲線另見 [`reactkey.md`](../../reactkey.md)。content 文案改動對 [`textstyle.md`](../../textstyle.md)。

---

## 範圍（八分即止）

**要看的**：本次 diff 是否破壞架構邊界、draw 熱路徑、registry 同步、視覺層級、讀者文案。

**不要在本 rule 裡重做的**：完整整合手冊、逐 slug 驗收表、把 Markdown 寫成 runtime spec。未改到的檔案不順手重構。

**輸出可簡化**：變更極小（文案 typo、單行 CSS）時，可省略十維度評分表，只列問題與是否可合併。

**全專案審查**（使用者明確要求時）：以 `src/` + `src/content/` + registry 為範圍；先跑 `npm test` / `npm run build`；按 §1–§8 掃描**系統性**問題，而非只看 git diff。已知例外（Julia 引擎、p5 型別在 module 內）在 §1 註明，其餘仍照規則判。

---

## 1. 架構邊界

- **Works**：`CurveModule` → `*CurveRoot` → portal → `work-detail__stage`
- **Explore**：`src/explore/*` + `*ExploreRoot`；**不用** CurveModule / portal
- `curve/modules/*`：**零 p5**；渲染在 `systems/rendering/*`；renderer 只收 snap，不讀 React state
- 新互動：`registry.ts` + `interactiveRegistry.ts` + `WorkInteractiveStage` 三處同步（`registry.sync.test` 會擋）
- 非 morph 的自訂引擎（如 Julia、相量動態）：可偏離 `useMorphCurveP5`，但仍遵守 hook 生命週期與 renderer 分層

## 2. p5 / React

- Instance mode：mount 一次，unmount `remove()`
- draw 內用 `useRef` 讀參數，避免 setState 風暴
- 離散參數（整數 a/b、R/r）vs 連續相位（δ）：該 reset 的 reset，該 lerp 的 lerp
- `byTheta` 禁止 `% TWO_PI`；渲染常數勿從 `curve/constants` import

## 3. 視覺（art.md）

- 只換 geometry，不重做 glow / grid / hierarchy
- 背景 `#0a0a0a`；accent 金；guide 低 alpha；ghost 弱於主軌跡

## 4. 完整性

- 邊界：空點列、零除、極端參數
- slug 頁掛載、`controls` id、縮圖 registry 是否齊

## 5. 可維護性

- **最小 diff**；`*CurveRoot` 薄包裝
- 重複渲染邏輯優先抽到 `systems/rendering`，但勿為抽而抽

## 6. 性能

- 參數變更才 rebuild path/cache；draw 內避免配置 DOM、大分配
- 列表縮圖走建置期 `curveThumbnail`，勿在 client 重算給卡片

## 7. Content / Astro

- works 文案：`互動說明` 寫讀者語言，工程詞（lerp、tile、Ghost）勿進 content
- 排序、`category` enum、`coverImage` 路徑符合現有慣例

## 8. 測試

- 純函數可測則測；至少 `npm run build`（涉及互動時目視一個作品頁）

---

## 問題格式

`[CRITICAL/MAJOR/MINOR] 檔案:行號 — 問題 → 建議`

| 嚴重度 | 典型 |
|--------|------|
| CRITICAL | 架構穿透、記憶體洩漏、registry 漏登導致建置/互動壞掉 |
| MAJOR | 違反分層、draw 每幀重算、視覺層級錯 |
| MINOR | 命名、註解、可讀性 |

## 輸出（預設）

1. 問題清單（CRITICAL → MAJOR → MINOR）
2. 優先修復 3～5 項
3. 可合併判定：無 CRITICAL；MAJOR 已修或接受；build 通過

大 PR 可加十維度評分表；小改可省略。
