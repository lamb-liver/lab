# Production only. Leave empty locally so dev / preview are not tracked.
# Create a site at https://cloud.umami.is for lab.lambliver.dev, then set this
# on the Vercel project (Production). data-domains already blocks other hosts.
PUBLIC_UMAMI_WEBSITE_ID=
PUBLIC_UMAMI_SCRIPT_URL=https://cloud.umami.is/script.js
