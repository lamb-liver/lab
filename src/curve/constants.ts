/** 邏輯畫布基準邊長（resize 時可等比縮放） */
export const BASE_CANVAS_SIZE = 600;

/** 角度採樣步長；不同曲線可覆寫 */
export const BASE_POINT_STEP = 0.006;

export const TWO_PI = Math.PI * 2;

/** 單次 sample 點數上限（draw 熱路徑） */
export const MAX_SAMPLE_POINTS = 8000;
