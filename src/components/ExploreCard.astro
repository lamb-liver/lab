---
import '../styles/components/card.css';
import CardThumbFallback from './CardThumbFallback.astro';
import type { ExploreCategory } from '../content.config';

interface Props {
  slug: string;
  title: string;
  description: string;
  category: ExploreCategory;
  coverImage?: string;
  featured?: boolean;
}

const { slug, title, description, category, coverImage, featured = false } = Astro.props;
---

<article
  class:list={['card', 'card--explore', { 'card--featured': featured }]}
  data-filter-item
  data-filter-category={category}
  data-search-slug={slug}
>
  <a href={`/explore/${slug}`} class="card__link-wrap">
    <span class="card__kind card__kind--explore">互動視覺化</span>
    <div class="card__thumb" aria-hidden="true">
      {
        coverImage ? (
          <img
            src={coverImage}
            alt=""
            class="card__thumb-cover"
            loading="lazy"
            decoding="async"
          />
        ) : (
          <CardThumbFallback />
        )
      }
    </div>
    <div class="card__body">
      <div class="card__title-row">
        <h3 class="card__title">{title}</h3>
        {featured ? <span class="card__featured-label">精選</span> : null}
      </div>
      <div class="card__tags">
        <span class="card__tag">{category}</span>
      </div>
      <p class="card__desc">{description}</p>
    </div>
  </a>
</article>
