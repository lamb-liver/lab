---
import '../styles/components/canvas.css';

interface Props {
  class?: string;
  label?: string;
}

const { class: className = 'canvas-placeholder', label = 'canvas 載入中' } =
  Astro.props;
---

<div class:list={[className]} role="img" aria-label={label}>
  {label}
</div>
